rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Permite ver las imágenes a todo el mundo
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Solo Admin sube fotos
    }
  }
}